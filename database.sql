create database kkeluarga;

use kkeluarga;

CREATE TABLE `kk` (
  `id` int(11) NOT NULL auto_increment,
  `name` varchar(100),
  `alamat` varchar(100),
  `nokk` varchar(30),
  `noktp` varchar(30),
  PRIMARY KEY  (`id`)
); 